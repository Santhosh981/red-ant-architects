# Match the opening reference and upgrade Capabilities

## What will change
- Rebuild the opening as a clean black, full-screen floating image field matching the supplied reference: one dominant central architecture image, varied rectangular/rounded/circular crops around it, subtle depth, and a controlled forward transition into the existing RED ANT reveal.
- Remove the current busy 3D fly-through treatment while retaining the existing handoff into the homepage and leaving every later page animation unchanged.
- Increase the Capabilities service-name scale for stronger editorial impact.
- Pair every capability with an architecture image. As each row enters, its image rises from below, briefly expands into view, then descends and disappears as the next capability takes over—an architectural sunrise/sunset motion tied to scroll.
- Keep the mobile version lighter and stacked, and provide an immediate readable state for reduced-motion visitors.

## Technical details
- Reuse the existing local architecture imagery; the uploaded screenshot is reference only.
- Keep the intro timing centralized in its existing configuration.
- Add one image mapping per service and a dedicated media layer inside the Capabilities section.
- Extend the existing GSAP/ScrollTrigger setup only for the new Capabilities image sequence.
- Verify the opening and Capabilities at desktop and mobile sizes, plus reduced-motion behavior and the current build status.
